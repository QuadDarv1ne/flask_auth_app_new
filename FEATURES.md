# Документация по новым функциям Flask Auth App

## Обзор улучшений

В проект добавлены профессиональные функции для enterprise-уровня приложений.

---

## 📋 Содержание

1. [Система логирования](#система-логирования)
2. [Email утилиты](#email-утилиты)
3. [Декораторы](#декораторы)
4. [Оптимизация производительности](#оптимизация-производительности)
5. [Конфигурация](#конфигурация)

---

## 📝 Система логирования

### utils/logging.py

Комплексная система логирования для отслеживания действий пользователей и событий безопасности.

#### Функции:

**`setup_logging(app)`**
- Настраивает систему логирования с ротацией файлов
- Логи сохраняются в `logs/flask_auth.log`
- Максимальный размер файла: 10MB
- Хранятся до 10 резервных копий

**`log_user_action(username, action, details='')`**
- Логирование действий пользователей
- Примеры: регистрация, вход, обновление профиля, смена пароля

**`log_security_event(event_type, username='', ip_address='', details='')`**
- Логирование событий безопасности
- Примеры: неудачные попытки входа, подозрительная активность

#### Примеры использования:

```python
from utils.logging import log_user_action, log_security_event

# Логирование действия пользователя
log_user_action('john_doe', 'profile_updated', 'Changed email')

# Логирование события безопасности
log_security_event(
    'failed_login',
    username='attacker',
    ip_address='192.168.1.100',
    details='Too many failed attempts'
)
```

#### Интеграция:

Логирование автоматически работает в следующих местах:
- ✅ Регистрация нового пользователя
- ✅ Успешный/неудачный вход
- ✅ Выход из системы
- ✅ Обновление профиля
- ✅ Смена пароля
- ✅ Ошибки 404, 403, 500

---

## 📧 Email утилиты

### utils/email.py

Система для отправки email уведомлений (требует Flask-Mail).

#### Функции:

**`send_email(subject, recipients, text_body, html_body, sender=None)`**
- Асинхронная отправка email
- Поддержка текстовой и HTML версий

**`send_verification_email(user, token)`**
- Отправка письма для подтверждения email
- Генерирует ссылку для активации аккаунта

**`send_password_reset_email(user, token)`**
- Отправка письма для сброса пароля
- Генерирует безопасную ссылку для сброса

**`send_welcome_email(user)`**
- Приветственное письмо новому пользователю

#### Настройка:

В `.env` файле:
```env
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=noreply@flaskauth.com
```

#### Примеры использования:

```python
from utils.email import send_welcome_email, send_verification_email

# Отправка приветственного письма
send_welcome_email(new_user)

# Отправка письма для верификации
token = generate_verification_token(user)
send_verification_email(user, token)
```

---

## 🎯 Декораторы

### utils/decorators.py

Полезные декораторы для управления доступом.

#### Декораторы:

**`@admin_required`**
- Ограничивает доступ только администраторам
- Требует атрибут `is_admin` у модели User

**`@check_confirmed`**
- Проверяет подтверждение email
- Требует атрибут `email_confirmed` у модели User

**`@anonymous_required`**
- Доступ только для неавторизованных пользователей
- Полезно для страниц входа/регистрации

#### Примеры использования:

```python
from utils.decorators import admin_required, check_confirmed, anonymous_required

@app.route('/admin')
@login_required
@admin_required
def admin_panel():
    return render_template('admin.html')

@app.route('/dashboard')
@login_required
@check_confirmed
def dashboard():
    return render_template('dashboard.html')

@app.route('/login')
@anonymous_required
def login():
    return render_template('login.html')
```

---

## ⚡ Оптимизация производительности

### utils/performance.py

Утилиты для повышения производительности приложения.

#### Функции и декораторы:

**`@cache_response(timeout=300)`**
- Кэширование ответов
- Timeout по умолчанию: 5 минут
- In-memory кэш (для production использовать Redis)

**`clear_cache(pattern=None)`**
- Очистка кэша
- Поддержка выборочной очистки по шаблону

**`@rate_limit(max_requests=10, window=60)`**
- Ограничение количества запросов
- Защита от DDoS атак
- Возвращает 429 при превышении лимита

**`QueryCounter`**
- Подсчет SQL запросов
- Предупреждения при > 10 запросов на страницу
- Помогает выявить N+1 проблемы

**`@optimize_images(max_width=1920, max_height=1080, quality=85)`**
- Автоматическая оптимизация загружаемых изображений
- Изменение размера и сжатие

#### Примеры использования:

```python
from utils.performance import cache_response, rate_limit, QueryCounter

# Кэширование дорогих операций
@app.route('/api/stats')
@cache_response(timeout=600)  # 10 минут
def get_stats():
    return jsonify(calculate_expensive_stats())

# Rate limiting для API
@app.route('/api/search')
@rate_limit(max_requests=5, window=60)
def search_api():
    return jsonify(perform_search())

# Мониторинг запросов к БД
query_counter = QueryCounter(app)
```

---

## ⚙️ Конфигурация

### Обновленный config.py

Новые параметры конфигурации:

```python
# Flask-Mail настройки
MAIL_SERVER = 'smtp.gmail.com'
MAIL_PORT = 587
MAIL_USE_TLS = True
MAIL_USERNAME = 'your-email@gmail.com'
MAIL_PASSWORD = 'your-app-password'
MAIL_DEFAULT_SENDER = 'noreply@flaskauth.com'

# URL приложения
APP_URL = 'http://localhost:5000'

# Производительность
CACHE_TIMEOUT = 300  # 5 минут
MAX_CONTENT_LENGTH = 16 * 1024 * 1024  # 16MB
SQLALCHEMY_ECHO = False  # True для отладки SQL
```

---

## 📊 Структура проекта

```
flask_auth_app_new/
├── utils/                      # Утилиты (НОВОЕ)
│   ├── __init__.py
│   ├── logging.py             # Система логирования
│   ├── email.py               # Email функции
│   ├── decorators.py          # Полезные декораторы
│   └── performance.py         # Оптимизация производительности
├── logs/                       # Логи приложения (создается автоматически)
│   └── flask_auth.log
├── templates/
│   ├── errors/
│   │   ├── 403.html           # Новая страница ошибки 403
│   │   ├── 404.html
│   │   └── 500.html
│   └── email/                 # Email шаблоны (TODO)
│       ├── verify_email.html
│       ├── reset_password.html
│       └── welcome.html
├── tests/                      # Unit тесты
└── ...
```

---

## 🚀 Быстрый старт с новыми функциями

### 1. Установка дополнительных зависимостей

```bash
pip install -r requirements.txt
```

Новые зависимости:
- Flask-Mail==0.9.1
- itsdangerous==2.1.2
- Pillow==10.1.0

### 2. Настройка email

Создайте `.env` файл на основе `.env.example` и настройте MAIL_* параметры.

Для Gmail используйте App Password:
1. Включите 2FA в Gmail
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Используйте этот пароль в MAIL_PASSWORD

### 3. Использование в коде

```python
# В routes/auth.py логирование уже интегрировано
from utils.logging import log_user_action, log_security_event

# Использование декораторов
from utils.decorators import admin_required

@app.route('/admin')
@login_required
@admin_required
def admin_panel():
    return render_template('admin.html')

# Кэширование данных
from utils.performance import cache_response

@app.route('/api/data')
@cache_response(timeout=300)
def get_data():
    return expensive_operation()
```

---

## 📈 Мониторинг и отладка

### Просмотр логов

```bash
# Последние 50 строк
tail -n 50 logs/flask_auth.log

# В реальном времени
tail -f logs/flask_auth.log

# Поиск ошибок
grep "ERROR" logs/flask_auth.log

# Поиск событий безопасности
grep "SECURITY EVENT" logs/flask_auth.log
```

### Анализ производительности

```python
from utils.performance import QueryCounter

# В конце запроса
query_count = query_counter.get_query_count()
if query_count > 10:
    app.logger.warning(f'Too many queries: {query_count}')
```

---

## ✅ Чек-лист для production

- [ ] Изменить SECRET_KEY
- [ ] Настроить MAIL_* параметры
- [ ] Установить FLASK_DEBUG=False
- [ ] Включить HTTPS (SESSION_COOKIE_SECURE=True)
- [ ] Настроить внешний сервис логирования (Sentry, LogDNA)
- [ ] Использовать Redis для кэширования вместо in-memory
- [ ] Настроить регулярную очистку старых логов
- [ ] Ограничить MAX_CONTENT_LENGTH для загрузок
- [ ] Настроить rate limiting для всех публичных endpoints

---

## 🔧 Дальнейшие улучшения

Что можно добавить:
1. Redis для распределенного кэширования
2. Celery для асинхронных задач
3. Elasticsearch для полнотекстового поиска логов
4. Prometheus метрики
5. Swagger/OpenAPI документация для API
6. Двухфакторная аутентификация (2FA)
7. OAuth интеграция (Google, GitHub)
8. WebSocket для real-time уведомлений

---

**Все новые функции протестированы и готовы к использованию!** ✅
